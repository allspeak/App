NEW RELEASE : versionCode="10", versionName="0.3.0"

NEW FEATURE:  NOISE command is treated differently from the other one.

NEW FEATURES: defined new techniques to improve recognition

 - nRecognitionDistance			=> minimum % difference between first and second command. if > of nRecognitionDistance => ok, otherwise playback "repeat the command"
								   default value is set in TFSrv::fixTfModel, taken from InitAppSrv.config.defaults.user.nRecognitionDistance
 - reset VAD threshold
 - set three different VADParams.nSpeechDetectionThreshold values to improve speech extraction in noisy environments.

NEW BEHAVIOUR: fRecognitionThreshold is managed in the setting-recognition page

BUG-FIX: in VocabularySrv::recoverMissingVocJsonFile...wrong sLabel

BUG-FIX & OPTIMIZATION in VocabulariesCtrl :  recover process now works


VocabularySrv RECODED
 - ADDED: 		getFullTrainVocabularyName
 - MODIFIED:	getTrainVocabularyVoicesPaths			returns [{id, filepath}]
				getExistingTrainVocabularyVoicesPaths	according to the above changes
				getStatus(voc, net)						returns only a status object given both the voc and net object
 - REMOVED:		getValidTrainVocabularies


RuntimeStatusSrv RECODED:
 - MODIFIED:	getStatus								returns {voc, net, ...}
				loadVocabulary							manages also the selected net





NEW METHODS - REFACTORING  in MiscellaneousSrvObj2ArrJSON         
	ADDED:
 - Obj2ArrJSON,
 - cloneObj            
 - cloneObjs           
 - cloneObjArray       


NEW TEXTS:     
	UITextSrv.RECOGNITION.labelUncertainResults                   = "ripeti il comando";
    UITextSrv.RECOGNITION.labelStartRecognition                   = "AVVIA RICONOSCIMENTO";
    UITextSrv.RECOGNITION.labelStopRecognition                    = "INTERROMPI RICONOSCIMENTO";

NEW ENUMS:  VAD enums & labeld are not defined in EnumSrv
				